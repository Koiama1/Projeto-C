#include <stdio.h>
#include "funcao.h"

int main() {
    // Implemente a lógica para carregar dados do cliente e as funções de débito, depósito, extrato e transferência.
    // ...
    struct Cliente clientes[100]; // Tamanho máximo para clientes
    int numClientes = 0;

    struct Funcionario funcionarios[100]; // Tamanho máximo para funcionários
    int numFuncionarios = 0;

    // Implemente a lógica para carregar dados de funcionários e clientes, bem como as funções de novo_funcionario, novo_cliente e login.
    // ...

    menu_login();


    return 0;
}
